import { createContext } from 'react';

const sellDomainCtx = createContext({
    
});
const sellDomainProvider = sellDomainCtx.Provider;

export default sellDomainCtx;
export { sellDomainProvider };
